package com.svb.fieldops.ui.theme

import androidx.compose.ui.graphics.Color

// SVB Field Ops — design tokens (hex)

val SvbBlack = Color(0xFF232323)
val SvbWhite = Color(0xFFFFFFFF)

val SvbPrimary1 = Color(0xFFF2B60B)
val SvbPrimary2 = Color(0xFFFFD000)
val SvbPrimary3 = Color(0xFFFFCB4A)
val SvbPrimary4 = Color(0xFFFFE28A)
val SvbPrimary5 = Color(0xFFFFEDCD)

val SvbSuccess = Color(0xFF47B881)
val SvbDanger = Color(0xFFF64C4C)

val SvbN1 = Color(0xFF1F1F1F)
val SvbN2 = Color(0xFF4B4B4B)
val SvbN3 = Color(0xFFBEBEBE)
val SvbN5 = Color(0xFFE1E1E1)
/** Inset dividers on muted surfaces (~#E0E0E0). */
val SvbDivider = Color(0xFFE0E0E0)
val SvbN7 = Color(0xFFF5F5F5)

/** Login / geofence light surfaces (from Field Ops–aligned mockups). */
val SvbLoginBackground = Color(0xFFFFFCF5)

/** Clock-in preview lower sheet (warm off-white). */
val SvbClockInSheet = Color(0xFFFDFCF9)

/** Clock-in preview header strip. */
val SvbClockInHeader = Color(0xFF333333)

/** Selfie camera card / elevated surface on dark UI. */
val SvbSelfieCard = Color(0xFF2C2C2C)

/** Muted cards on home dashboards (Field Ops mockups ~ #F2F2F2). */
val SvbCardMuted = Color(0xFFF2F2F2)

/** Engineer “pending approvals” alert surface (soft rose). */
val SvbRoseTint = Color(0xFFFFF0F0)
